_: {
  # idk why but this is really annoying, I guess I can just keep it here if I ever loose the file or something
  # xdg.configFile."karabiner/karabiner.json".text = builtins.readFile ./karabiner.json;
}
